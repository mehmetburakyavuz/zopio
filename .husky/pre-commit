#!/usr/bin/env sh

echo "🔍 Running pre-commit checks..."

# Run lint-staged to check only staged files
pnpm lint-staged || { echo "❌ Lint-staged checks failed"; exit 1; }

# Run type checking on the entire project
#echo "🔎 Running type check..."
#pnpm typecheck || { echo "❌ Type checking failed"; exit 1; }

echo "✅ All checks passed!"
